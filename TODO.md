# TODO

- [x] Make the NLLB-translated output feed as smooth/instant as the untranslated RealtimeSTT feed. Untranslated finals go straight to screen via `_realtime_stt_show` (realtime_stt_mixin.py) — one direct append + render. Translated finals instead go through the word-by-word reveal queue (`enqueue_text`/`reveal_next_word` in display_mixin.py), which feels jerky next to the instant path. Match the NLLB feed's display behavior to the direct-commit style used for non-translated text.
- [x] Create a checkbox to autostart with Windows.
- [x] Remove non-MME audio options.
- [x] Clean up docs and remove orphaned references to removed engines/providers (Kroko, Omnilingual sidecar, standalone faster-whisper, OpenAI STT, OpenAI text translation) in README.md, AGENTS.md, and `.env.example` now that the app is RealtimeSTT + Local NLLB only.
- [x] Evaluate the Advanced settings panel (settings_ui_mixin.py) one setting at a time to determine which are never actually adjusted in practice and can be removed or hardcoded to their default value, to shrink the settings surface. Removed "Enable noise gate" / "Noise Cancellation (strength)" — confirmed dead, cosmetic-only meter marker that never gated real audio. Remaining Advanced settings (Chunk Size, Chunk Delay, Response Delay, Display Speed, Logging mode, Start-with-Windows, CUDA directory) are all actively consumed downstream.
- [ ] Buffer/paginate the output window so a long uninterrupted stretch of speech doesn't dump one large block of text that fills or overflows the display; needs a scrolling/windowing strategy for the transcript render (display_mixin.py).
- [x] Remove the live word-by-word interim preview entirely (realtime_stt_mixin.py). With translation off it caused visible jerkiness — every ~100ms partial update forced a full re-wrap/reflow of the display. Only finalized RealtimeSTT output is shown now, regardless of translation state.
- [x] Turn the RealtimeSTT and Local NLLB model-name settings into dropdown menus, with recommended VRAM/system requirements shown next to each option. Final/realtime STT model and NLLB model name are now `tk.OptionMenu` dropdowns labeled with approximate VRAM/disk needs, mapped back to their underlying model-id strings (settings_ui_mixin.py).
- [x] Rename the "API" settings section to "Speech-to-Text" or "Transcription" now that it only holds RealtimeSTT/NLLB model settings, not API keys. Renamed to "Transcription" — reads as plain language and pairs naturally with the "Translation" section below it. Also renamed the internal `api_section`/`api_vars`/`_build_api_section`/`_apply_api_vars` identifiers to `transcription_*` for consistency (settings_ui_mixin.py).
- [x] Move the Bad Words filter section into Advanced settings. Bad Words and Custom Vocabulary now share one "Filters" subsection inside Advanced (settings_ui_mixin.py); the old standalone top-level "Filters" section is gone.
- [x] Re-evaluate whether to keep post-speech silence, Silero sensitivity, Local GPU Runtime, etc. as user-facing settings, or hardcode/remove them. Text Manipulation (Chunk Size, Chunk Delay, Response Delay, Display Speed) confirmed still load-bearing downstream (display_mixin.py/transcription_mixin.py) but rarely worth end-user tuning — hardcoded to their prior defaults (120 chars / 90ms / 100ms / 1.0x) and removed the "Text Manipulation" settings section and its config load/save keys (settings_ui_mixin.py, settings_mixin.py). Post-speech silence: confirmed dead as a user-facing knob — RealtimeSTT overwrites it dynamically (`_realtime_stt_adjust_silence`, realtime_stt_mixin.py) within ~200ms of any speech, so the slider was cosmetic; removed the setting/UI and folded the initial constructor value into the existing `_SILENCE_*` constants as `_SILENCE_INITIAL = 0.6`. Silero sensitivity and Local GPU Runtime (CUDA directory) are both genuinely load-bearing and machine/environment-specific — kept as-is.
- [ ] Add an alternative display option that overlays 1-2 lines of transcription/translation at the bottom of a live OBS/RTSP stream feed (or another researched-and-vetted streaming approach), similar to Facebook Live captions.
- [ ] Check Spanish bad words to make sure they are in a compatible format, such as symbols and such.
- [ ] Add support for the 99 RealtimeSTT languages and the 200 NLLB languages. Also add a tooltip hint showing which languages are supported for RealtimeSTT.
