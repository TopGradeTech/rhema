# Project instructions

The standards for this project live in `ARCHITECTURE.md`, which is the single
source of truth for architecture, commands, conventions, and the release
process. This file exists only so they are picked up automatically — it
deliberately holds no content of its own, so the two can never disagree.

@ARCHITECTURE.md
