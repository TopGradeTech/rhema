speechrecognition
pyaudio
# ttkbootstrap 2.1+ specifically: the app names its own container colors as
# @#hex "surface" bootstyles (see _surface_bootstyle) so ttkbootstrap
# composites rounded widget edges against the real background instead of the
# theme's, and 2.x renamed the themes this app asks for (bootstrap-light /
# bootstrap-dark). An older 2.0 would not error - it warns and silently falls
# back, reappearing as the cosmetic halo this pin exists to prevent.
ttkbootstrap>=2.1
faster-whisper
transformers
sentencepiece
# Plain `torch` here resolves to the CPU-only build from PyPI, which is the
# right default: it works everywhere and needs no CUDA install.
#
# For GPU, install a CUDA build INSTEAD, after installing this file. Releases
# are built against cu124, so that is the best-tested choice:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# IMPORTANT: a CUDA torch alone only accelerates translation (NLLB). Speech
# recognition runs on faster-whisper -> CTranslate2, which bundles no CUDA of
# its own and needs the NVIDIA driver plus CUDA Toolkit 12.x and cuDNN 9.x for
# CUDA 12 installed on the system. Install those first, or transcription
# silently stays on CPU (or fails) while the app still reports "cuda", because
# the device check uses torch as a proxy for both engines. See the README.
torch
# RealtimeSTT — local real-time transcription with dual VAD + two-pass models.
#
# Pinned to our fork's `faster-whisper-engine-options` branch, and this is
# REQUIRED rather than a nicety: realtime_stt_mixin.py passes
#   transcription_engine_options={"model": {"local_files_only": True}}
# to keep startup fully offline. Upstream *accepts* that argument but its
# faster_whisper adapter silently ignores it, so on stock upstream the app
# reaches out to Hugging Face on every launch and raises no error to explain
# why — a silent behavior regression rather than a visible failure. The fork
# only adds that pass-through (see its commit "Honor engine_options in the
# faster_whisper engine"); it is otherwise stock RealtimeSTT 1.0.2.
#
# Once the upstream PR lands, drop the URL and pin the release that has it.
# For a GPU build, add the extra: RealtimeSTT[gpu] @ git+https://...
#
# Developing against the fork itself? Install it editable instead of this line:
#   git clone -b faster-whisper-engine-options https://github.com/TopGradeTech/RealtimeSTT.git
#   pip install -e path/to/RealtimeSTT
RealtimeSTT @ git+https://github.com/TopGradeTech/RealtimeSTT.git@faster-whisper-engine-options
# Ships the Silero VAD ONNX models locally so RealtimeSTT's auto backend
# never needs a torch.hub download at runtime (offline-first, and avoids a
# fresh-install failure with no local model file to fall back to)
silero-vad
# Video Feed mode — decodes the OBS Virtual Camera to show behind captions
opencv-python
pillow
# Real camera friendly names ("OBS Virtual Camera") for the Camera Device
# dropdown instead of bare index numbers — Windows-only, DirectShow-based
pygrabber; sys_platform == "win32"
# pywebview port (main_webview.py / WebTranslationApp) — Windows backend
# (platforms/winforms.py) needs pythonnet for its WinForms/.NET interop;
# webview_bridge.py's invoke_on_ui_thread() imports clr/System directly for
# the same reason. main.py/TranslationApp (the Tk app) does not use either.
pywebview>=6.2.1
pythonnet>=3.1.0; sys_platform == "win32"
# Controller's Preview/Output Snapshot - occlusion-independent per-HWND
# capture via Windows Graphics Capture (the same API OBS/Xbox Game Bar use
# for "Window" capture). Replaces two rejected techniques: bbox/BitBlt
# (captures whatever's on top of that screen region, a real information-
# disclosure risk - confirmed twice against real unrelated windows during
# development) and PrintWindow, the real Tk app's own technique (returns
# solid black against a WebView2 surface). See web_settings_ui_mixin.py's
# _capture_output_snapshot_data_uri for the full accounting.
windows-capture; sys_platform == "win32"
